# Ritual v2 (Craving Deck + Nutrition + Breakfast) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add per-serve protein+calories everywhere a dish is named, a rotating high-protein breakfast set, and turn ritual Touchpoint 1 into an 8-card craving deck.

**Architecture:** This feature is almost entirely **prompt + state-file engineering** — no new Python, no new daemon jobs. Edits land in the `plan-week` SKILL (the ritual brain), `prompts/chat.md`, and `state/` files. Per this project's "verify via real use" rule, verification is exact-edit inspection + a real ritual run, not pytest (there is no unit-testable code here).

**Tech Stack:** Markdown prompts, markdown state files. Spec: `docs/superpowers/specs/2026-06-15-alfred-ritual-nutrition-breakfast-design.md`.

**Conventions for this plan:** "Modify" steps show the exact existing text to find and the exact replacement. Re-read the changed section after each edit to confirm coherence. Commit after each task.

---

### Task 1: Preferences — nutrition defaults + breakfast week-shape note

**Files:**
- Modify: `state/preferences.md:15-23`

- [ ] **Step 1: Replace the Nutrition defaults block**

Find:
```
## Nutrition defaults
- Every dinner anchored on a protein source, ~30–40 g/serve
- Whole-food bias; no macro tracking in v1
```
Replace with:
```
## Nutrition defaults
- Every dinner anchored on a protein source, ~30–40 g/serve
- Show per-serve **protein (g) + calories** on every recipe card, the weekly
  summary, and each craving-deck card — LLM-estimated, directional, marked `~`.
  Per serve = per person (recipes are 2人份).
- Mike is cutting fat → favor high-protein, lean dinners. Calories are
  informational; no hard target in v1.
- Whole-food bias.
```

- [ ] **Step 2: Replace the Week shape defaults' first bullet**

Find:
```
## Week shape defaults
- Dinners only; default 6 dinners/week (confirm count each ritual)
```
Replace with:
```
## Week shape defaults
- Dinners planned weekly; default 6/week (confirm count each ritual). Breakfast is
  a separate **rotating go-to set** (`state/breakfasts.md`) — not planned
  day-by-day, just provisioned onto the weekly shopping list.
```

- [ ] **Step 3: Verify** — Read `state/preferences.md`; confirm both blocks read coherently and no stray "no macro tracking" / "Dinners only" remains.

- [ ] **Step 4: Commit**
```bash
git add state/preferences.md
git commit -m "feat(ritual): preferences — show per-serve P+cal; breakfast as rotating set"
```

---

### Task 2: Nutrition — recipe card + summary formats + Step 7 estimate-and-store

**Files:**
- Modify: `.claude/skills/plan-week/SKILL.md` (recipe-format template ~line 112; summary day-line ~line 169; Step 7 ~line 178)

- [ ] **Step 1: Add the macro line to the recipe-format template**

Find:
```
🍳 **{Day} — {菜名}**  ({mode} · 約{min}分 · 2人份)

**🔪 備料(開火前全部完成 — mise en place)**
```
Replace with:
```
🍳 **{Day} — {菜名}**  ({mode} · 約{min}分 · 2人份)
🔢 每份 ~{P}g 蛋白 · ~{kcal} kcal

**🔪 備料(開火前全部完成 — mise en place)**
```

- [ ] **Step 2: Add macros to the summary day-line format**

Find:
```
📅 **The Week — {date range}**
Mon · {dish} ({mode}, {min}m)
Tue · {dish} ({mode}, {min}m)
```
Replace with:
```
📅 **The Week — {date range}**
週一 · {dish} ({mode}, {min}分 · ~{P}P/{kcal}kcal)
週二 · {dish} ({mode}, {min}分 · ~{P}P/{kcal}kcal)
```

- [ ] **Step 3: Tell Step 7 to estimate and store macros for new dishes**

Find (Step 7, the new-dish bullet):
```
- For each NEW dish: create `state/cookbook/<slug>.md` with the full recipe and
  an empty `## Verdicts` section. Existing dishes: add `- planned YYYY-MM-DD`.
```
Replace with:
```
- For each NEW dish: create `state/cookbook/<slug>.md` with the full recipe
  (including the `🔢 每份 ~{P}g 蛋白 · ~{kcal} kcal` line under the title — estimate
  per-serve protein+calories from the ingredients & portion) and an empty
  `## Verdicts` section. Existing dishes: add `- planned YYYY-MM-DD`.
```

- [ ] **Step 4: Verify** — Read the three edited regions of `SKILL.md`; confirm the macro line/format appears in the recipe template, the summary template, and Step 7.

- [ ] **Step 5: Commit**
```bash
git add .claude/skills/plan-week/SKILL.md
git commit -m "feat(ritual): show per-serve protein+calories on cards & summary"
```

---

### Task 3: Nutrition — backfill macro lines into existing cookbook cards

**Files:**
- Modify: every `state/cookbook/*.md` (15 files)

- [ ] **Step 1: Backfill each recipe**

For each file in `state/cookbook/`, read it, estimate per-serve protein+calories from its ingredients/portion, and insert this line immediately **after the H1/title line** (the `🍳 **…**` or `# …` first line), before the next line:
```
🔢 每份 ~{P}g 蛋白 · ~{kcal} kcal
```
Example — `state/cookbook/steak-zucchini.md` line 1 is `🍳 **週三 — 牛排 + 蒜香節瓜** …`; insert `🔢 每份 ~46g 蛋白 · ~540 kcal` as the new line 2 (steak is high-protein, butter-basted). Estimate each dish honestly; mark `~`.

- [ ] **Step 2: Verify count**
```bash
grep -L "每份" state/cookbook/*.md
```
Expected: **no output** (every file now has a macro line). If any file is listed, it was missed — add its line.

- [ ] **Step 3: Commit**
```bash
git add state/cookbook/
git commit -m "feat(ritual): backfill per-serve P+cal onto existing cookbook cards"
```

---

### Task 4: Breakfast — seed `state/breakfasts.md`

**Files:**
- Create: `state/breakfasts.md`

- [ ] **Step 1: Create the seed set** (all fish-free per gf's no-fish; no eggplant/coriander/celery/century-egg; high-protein, quick)

```markdown
# 早餐輪替 — Breakfast rotation
<!-- Stable rotating set of quick high-protein breakfasts. Not planned day-by-day;
     its recurring ingredients are provisioned onto the weekly Woolies list by the
     ritual (Step 6). Edit via chat (「早餐加 X / 換掉 Y」 → captured → applied at the
     next ritual). High-protein bias — Mike is cutting fat. All fish-free. -->

### 希臘優格高蛋白碗
~30g 蛋白 · ~350 kcal · 5分
- 食材:無糖希臘優格、莓果、燕麥、堅果、蜂蜜
- 做法:優格鋪底,撒燕麥、莓果、堅果,淋一點蜂蜜。

### 蔥花炒蛋 + 全麥吐司
~25g 蛋白 · ~380 kcal · 10分
- 食材:蛋3顆、蔥花、全麥吐司2片、奶油
- 做法:蛋打散加蔥花,中小火炒到剛凝固即起;吐司烤香抹薄奶油。

### 雞胸蔬菜歐姆蛋
~35g 蛋白 · ~400 kcal · 12分
- 食材:雞胸丁、蛋3顆、彩椒/菠菜、少許起司
- 做法:雞胸丁先煎熟,倒入蛋液與蔬菜,半凝固時對折包起。

### 高蛋白隔夜燕麥
~35g 蛋白 · ~450 kcal · 隔夜(5分準備)
- 食材:燕麥、牛奶、乳清蛋白粉、香蕉、花生醬
- 做法:全部拌勻冷藏隔夜;早上撒香蕉與一匙花生醬。

### 茅屋起司莓果堅果碗
~28g 蛋白 · ~320 kcal · 5分
- 食材:茅屋起司(cottage cheese)、莓果、堅果、少許蜂蜜
- 做法:起司鋪底,鋪莓果堅果,淋蜂蜜。
```

- [ ] **Step 2: Verify**
```bash
grep -c "^### " state/breakfasts.md
```
Expected: `5`

- [ ] **Step 3: Commit**
```bash
git add state/breakfasts.md
git commit -m "feat(breakfast): seed rotating high-protein breakfast set"
```

---

### Task 5: Breakfast — shopping provisioning, ritual reconcile, chat query + edit capture

**Files:**
- Modify: `.claude/skills/plan-week/SKILL.md` (Step 6 shopping; Step 1/7 reconcile)
- Modify: `prompts/chat.md` (breakfast query + edit capture)

- [ ] **Step 1: Add a Breakfast group to the shopping-list format**

In `SKILL.md`, find the shopping-list format block:
```
**Produce**
- {Woolworths product name} · {pack size} · x{qty}
**Meat & seafood**
...
**Dairy & fridge**
...
**Pantry**
...

(staples assumed: see anything missing, shout)
```
Replace with:
```
**Produce**
- {Woolworths product name} · {pack size} · x{qty}
**Meat & seafood**
...
**Dairy & fridge**
...
**Pantry**
...
**Breakfast**
- {recurring ingredients from state/breakfasts.md's active set, sized for the week}

(staples assumed: see anything missing, shout)
```

- [ ] **Step 2: Tell Step 6 how to build the Breakfast group**

In `SKILL.md` Step 6, immediately after the "Channel tags (v2)" paragraph, add:
```
**Breakfast provisioning:** Read `state/breakfasts.md`. List the active set's
weekly-consumed ingredients under the **Breakfast** group (e.g. Greek yogurt, oats,
berries, nuts, wholemeal bread, banana, peanut butter, whey protein, cottage
cheese, chicken breast). **Dedupe** against `state/staples.md` (eggs, honey, butter
are staples → omit) and against the dinner lines already on the list (don't double a
protein). Tag `[woolies]`/`[asianpantry]` like any other line.
```

- [ ] **Step 3: Tell the ritual to reconcile breakfast edits from the inbox**

In `SKILL.md` Step 1 (the inbox paragraph), find:
```
preference/correction lines may warrant
updating `state/preferences.md` and/or `state/woolworths.md` (handle these in
Step 2 / Step 7 as appropriate);
```
Replace with:
```
preference/correction lines may warrant
updating `state/preferences.md` and/or `state/woolworths.md` (handle these in
Step 2 / Step 7 as appropriate); **preference lines prefixed 「早餐輪替:」 update
`state/breakfasts.md`** (add/replace/remove a breakfast entry) in Step 7;
```

- [ ] **Step 4: Add breakfast query + edit capture to chat.md**

In `prompts/chat.md`, find the end of the read-only capabilities bullet about 料理求救 (line ~21, the `state/lessons.md` line). After that bullet, add a new bullet under "你現在能做的":
```
- **早餐**:對方問「今天早餐吃啥」之類 → 讀 `state/breakfasts.md`,挑一個建議(換著吃、可帶上蛋白/熱量)。對方想改早餐輪替(加一道、換掉、不要某道)→ 用 capture 記下,下次儀式會套用到 `state/breakfasts.md`:
  `uv run scripts/capture.py preference "早餐輪替:加入酪梨蛋吐司 (Mike)"`
  然後回覆說記下了、週日儀式會更新輪替。聊天本身不直接改 breakfasts.md(唯讀)。
```

- [ ] **Step 5: Verify** — Read the edited regions of `SKILL.md` and `chat.md`; confirm the Breakfast group, the provisioning paragraph, the inbox-reconcile clause, and the chat breakfast bullet all read coherently and reference `state/breakfasts.md` consistently.

- [ ] **Step 6: Commit**
```bash
git add .claude/skills/plan-week/SKILL.md prompts/chat.md
git commit -m "feat(breakfast): weekly provisioning, ritual reconcile, chat query+edit"
```

---

### Task 6: Craving deck — rewrite Touchpoint 1 (Step 3) + Step 5 read-picks

**Files:**
- Modify: `.claude/skills/plan-week/SKILL.md` (Step 3 ~lines 49-60; Step 5 ~lines 78-86)

- [ ] **Step 1: Replace the entire Step 3 block**

Find the block starting `## Step 3 — Inventory & intent (Touchpoint 1) — FLEXIBLE START` through the line ending `use it for this plan, never save it.` Replace the whole block with:
```
## Step 3 — Craving deck (Touchpoint 1)
Open with a **craving deck**, not "what's in your fridge?" — present 8 numbered
candidate dishes they react to. This kills the blank-page problem and injects
novelty + skill progression. Build the deck (target 8) from these buckets:
- 🔁 banger ×2 — cookbook dishes with 🔥/banger verdicts not cooked in ~4 weeks
- 💭 craving ×(up to 2) — this week's harvested channel + inbox cravings
- ✨ new ×3 — dishes NOT in the cookbook, fitting prefs (high-protein lean), genuinely novel
- 🥋 challenge ×1 — the next technique from `state/skills.md` (a `play` dish, 已排入/候選)

**Fallback** to keep the total at 8 when fewer than 2 cravings are saved this week:
0 cravings → backfill +1 banger +1 new; 1 craving → backfill +1 new (favor novelty).
First-ever / thin history (no bangers available) → fill entirely with new + challenge.

**HARD rules for the deck:** never include an allergen dish; avoid dislikes unless
explicitly requested; don't deck a dish cooked in the last 2 weeks unless it's a
requested banger. If they already dropped cravings / a fridge photo / inventory in
the channel, use it to seed the deck (their cravings → 💭 cards; inventory biases
picks toward using up perishables).

Each card is ONE line:
`{n}. {bucket emoji} {菜名} · {mode} · ~{min}分 · ~{P}P/{kcal}kcal — {一句 hook}`
(estimate new-dish macros on the fly). Post the deck as your reply, then **STOP**.
If allergies are still unknown, append 「(對了,有什麼一定不能吃的嗎?)」 to this message.

They reply with picks + optional vetoes + optional inventory, e.g.
「1 4 6 想吃 · 3不要 · 還有雞胸要用掉」. Fridge inventory is OPTIONAL now — don't chase
a photo, don't burn the touchpoint on it. Any snapshot they give stays **ephemeral**:
use it for this plan, never save it.
```

- [ ] **Step 2: Update Step 5 to build from the deck picks**

In `SKILL.md` Step 5, find:
```
Propose the COMPLETE week in one message, defaulting to 6 dinners — open with
"Planned 6 dinners — say the word if this week needs fewer." Do NOT ask the count
as a separate question.
```
Replace with:
```
Build the week from the deck's 👍 picks FIRST, then fill the remaining dinner slots
from staples/rotation/curriculum honoring the high-protein lean. Propose the
COMPLETE week in one message, defaulting to 6 dinners — open with "Planned 6
dinners — say the word if this week needs fewer." Do NOT ask the count as a
separate question.
```

- [ ] **Step 3: Verify** — Read `SKILL.md` Steps 3 and 5; confirm Step 3 is the deck (no leftover "FLEXIBLE START" inventory-first text), the two-touchpoint accounting still holds (TP1 = deck, TP2 = proposal), and Step 5 references the deck picks.

- [ ] **Step 4: Commit**
```bash
git add .claude/skills/plan-week/SKILL.md
git commit -m "feat(ritual): Touchpoint 1 becomes an 8-card craving deck"
```

---

### Task 7: Test checklist + integration verification

**Files:**
- Modify: `TEST-CHECKLIST.md`

- [ ] **Step 1: Add scenarios** — append a new section to `TEST-CHECKLIST.md`:
```
## Ritual v2 — craving deck + nutrition + breakfast
- [ ] N1 Deck: ritual TP1 posts 8 cards in blend 2🔁 + 2💭 + 3✨ + 1🥋, each with ~P/kcal
- [ ] N2 Deck fallback: 0 cravings → 3🔁 + 4✨ + 1🥋; 1 craving → 2🔁 + 1💭 + 4✨ + 1🥋
- [ ] N3 Deck honors allergies (none decked) + no last-2-weeks repeats
- [ ] N4 Picks build the week; Step 5 proposal reflects 👍 picks; two-touchpoint preserved
- [ ] N5 Nutrition: new recipe cards + summary lines + deck cards carry ~Pg/~kcal
- [ ] N6 Nutrition backfill: every state/cookbook/*.md has a 每份 line
- [ ] N7 Breakfast: weekly Woolies list grows a Breakfast group (deduped vs staples)
- [ ] N8 Breakfast chat: 「今天早餐吃啥」 returns a suggestion from breakfasts.md
- [ ] N9 Breakfast edit: 「早餐加 X」 → inbox preference 「早餐輪替:…」 → applied next ritual
```

- [ ] **Step 2: Commit**
```bash
git add TEST-CHECKLIST.md
git commit -m "docs: ritual v2 test scenarios (N1-N9)"
```

- [ ] **Step 3: Integration verification (real use)** — On the next ritual (laptop or 「排菜單」 in chat), confirm N1–N9 by observation. This is the project's verification model (no test harness); tick items in `TEST-CHECKLIST.md` as they pass during real weekly use.

---

## Notes for the executor
- These are tightly-coupled edits to a few shared files (especially `SKILL.md`), so **inline execution** fits better than fresh-subagent-per-task here.
- The listener re-reads prompts live (`build_prompt` reads each call), so chat.md changes take effect without a daemon restart; SKILL.md changes take effect on the next ritual.
- Do NOT touch the currently-locked week (`state/plans/2026-06-15.md`).
